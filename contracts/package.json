{
  "name": "minicard-contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network celo",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network celoSepolia",
    "migrate:mainnet": "npx hardhat run scripts/migrate.js --network celo",
    "verify": "npx hardhat verify --network celo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "2.1.3",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
